import { Injectable } from '@angular/core';

/**
 * @deprecated Exercise answers are now validated locally and submitted in bulk
 * via LessonService.submitLesson(). This service is kept as an empty stub to
 * avoid breaking any potential external imports until they are cleaned up.
 */
@Injectable({
  providedIn: 'root',
})
export class ExerciseService {}
